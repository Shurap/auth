import React from 'react';
import styles from './Landing.module.css';

const LandingPage = () => (
  <div className={styles.window}>
    <h2>Start page</h2>
    <h3>This test site allows you to register with Firebase, log into the database, and see all registered users.</h3>
  </div>
);

export default LandingPage;